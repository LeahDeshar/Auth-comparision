type User {
  id: ID!
  email: String!
  name: String!
  role: Role!
}

enum Role {
  USER
  ADMIN
}

type Query {
  me: User
}

# type Mutation {
#   login(email: String!, password: String!): String
#   register(email: String!, password: String!, name: String!): String
# }
